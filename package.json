{
  "name": "moltnet-breath",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test",
    "collect": "pnpm -r --filter './packages/city-*' run collect",
    "collect:vienna": "pnpm --filter @moltnet-breath/city-vienna run collect",
    "collect:paris": "pnpm --filter @moltnet-breath/city-paris run collect"
  }
}
